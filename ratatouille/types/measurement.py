"""Measurement enum."""
from enum import Enum


class Measurement(Enum):
    """Measurement represented as an enum."""

    ML = "ml"
    L = "l"
    G = "g"
    DG = "dg"
    KG = "kg"
    PIECE = "piece"
